apiVersion: v1
kind: Service
metadata:
    name: "board-api"
    labels:
        app: "board-api"
spec:
    type: LoadBalancer
    ports:
    - port: 8090
      targetPort: 8090
      protocol: TCP
      name: http
    selector:
        app: "board-api"
